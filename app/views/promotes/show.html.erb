<h2><%= @promote.name %></h2>
<br/><br/>
<%= url_to_image(700, @promote.image) %>  
<br/>
<%= @promote.description %>  

<h3><b>Товары, участвующие в акции:</b></h3>

<br />
<div class="row">
  <% @promo_items.each do |i|  %>
    <div class="col-md-3">
      <div class="thumbnail">
      <%= url_to_image(250, i.image) %>    
        <div class="caption">
          <h3><%= link_to i.name, item_path(i) %></h3>
        </div>
      </div>
    </div>
  <% end %>
</div>